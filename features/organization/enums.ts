export enum OrganizationStatus {
  Active = "active",
  Flagged = "flagged",
  Suspended = "suspended",
  Archived = "archived",
}

export enum OrganizationMemberRole {
  Owner = "owner",
  Admin = "admin",
  Member = "member",
}
